{% extends 'base.html.twig' %}

{% block title %}Tous les utilisateurs{% endblock %}

{% block body %}
<div class="container mx-auto mt-6">
    <div class="overflow-x-auto">
        <table class="table-auto w-full border-collapse">
            <thead>
                <tr class="bg-gray-100">
                    <th class="px-4 py-2">Prénom</th>
                    <th class="px-4 py-2">Nom</th>
                    <th class="px-4 py-2">Code postal</th>
                    <th class="px-4 py-2">Adresse</th>
                </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr class="border-b border-gray-200">
                    <td class="px-4 py-2 text-center">{{ user.firstname }}</td>
                    <td class="px-4 py-2 text-center">{{ user.lastname }}</td>
                    <td class="px-4 py-2 text-center">{{ user.zipcode }}</td>
                    <td class="px-4 py-2 text-center">{{ user.adress }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        </table>
    </div>
</div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
<div class="container mx-auto">
    <div class="max-w-md mx-auto mt-8">
        <div class="bg-white shadow-md rounded-lg overflow-hidden">
            <div class="p-4">
                <h1 class="text-2xl font-bold text-center mb-4">Profil</h1>
                <hr class="mb-4">
                <div class="flex flex-col mb-4">
                    <span class="text-gray-600">Prénom:</span>
                    <span class="text-black">{{ app.user.firstname }}</span>
                </div>
                <div class="flex flex-col mb-4">
                    <span class="text-gray-600">Nom:</span>
                    <span class="text-black">{{ app.user.lastname }}</span>
                </div>
                <div class="flex flex-col mb-4">
                    <span class="text-gray-600">Email:</span>
                    <span class="text-black">{{ app.user.email }}</span>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
